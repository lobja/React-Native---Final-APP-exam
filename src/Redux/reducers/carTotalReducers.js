export const carTotalReducers = (state = 0, action)=>{
        
                  return(
                       {
                         TotalItems : action.TotalItems
                       }
                  )
        
    }

